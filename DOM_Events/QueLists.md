# 🧩 DOM Manipulation Practice Set

---

## 🟩 **Question 1: Change Text on Button Click**

**🎯 Objective:**  
When a button is clicked, change the text of a `<p>` tag with id `"message"`.

**📝 Task:**  
On button click → The `<p>` element’s text should change to **“Hello JavaScript!”**

**💡 Topic Covered:**  
`Event Handling`, `textContent Manipulation`, `DOM Selection`

---

## 🟩 **Question 2: Dynamic List and Click Alerts**

**🎯 Objective:**  
Create and manage a dynamic `<ul>` list.

**📝 Task:**  
- When the **“Add Item”** button is clicked → A new `<li>` should be added to the end of the list.  
- When any `<li>` (whether old or newly created) is clicked → show its text in an `alert()`.

**💡 Topic Covered:**  
`Event Delegation`, `DOM Creation`, `appendChild()`, `Event Bubbling`

---

## 🟩 **Question 3: Dynamic Paragraph Creator**

**🎯 Objective:**  
Create multiple paragraphs dynamically on button click.

**📝 Task:**  
Each time the user clicks the button →  
Create a new `<p>` element with text **“New Paragraph created!”**  
and append it inside a `<div>` (each click adds one more paragraph).

**💡 Topic Covered:**  
`DOM Manipulation`, `createElement()`, `appendChild()`

---

## 🟩 **Question 4: Live Text Update (Real-Time Input Reflection)**

**🎯 Objective:**  
Mirror user input text into a paragraph in real time.

**📝 Task:**  
When the user types inside an `<input>` box →  
the same text should instantly appear in a `<p>` tag (without any button).

**💡 Topic Covered:**  
`Input Events`, `value Property`, `Real-Time Data Binding`

---

## 🟩 **Question 5: Toggle Dark Mode**

**🎯 Objective:**  
Implement a dark/light mode toggle using a button.

**📝 Task:**  
- Create a button labeled **“Toggle Dark Mode”**  
- On each click → toggle the page’s background and text color between **dark mode** and **light mode**  
- Use `classList.toggle()` to switch styles efficiently.

**💡 Topic Covered:**  
`classList.toggle()`, `CSS Manipulation`, `Theme Switching`

---

## 🟩 **Question 6: Click Counter with Reset and Color Change**

**🎯 Objective:**  
Create an interactive click counter with reset and visual feedback.

**📝 Task:**  
- Create a button labeled **“Clicked 0 times”**  
- Each click increments the count displayed in the button text  
- Add a **Reset** button that sets the count back to 0  
- **(Bonus):** Change the button’s color every 5 clicks for a fun effect

**💡 Topic Covered:**  
`Event Listeners`, `Counter Logic`, `Dynamic Styling`, `textContent Update`

---

## 🟩 **Question 7: Counter with +, –, and Reset Buttons**

**🎯 Objective:**  
Build a simple counter interface with three buttons.

**📝 Task:**  
- Three buttons: **+**, **–**, and **0 (Reset)**  
- A `<p>` tag shows the current count  
- Clicking + → increments  
- Clicking – → decrements  
- Clicking 0 → resets the counter to zero  

**💡 Topic Covered:**  
`Event Handling`, `DOM Updates`, `Increment-Decrement Logic`

---


